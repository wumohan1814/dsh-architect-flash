name: Architect-Flash（架构师模式）
description: Two-tier Pro/Flash coding preset — plan and review on the main model, implement low-level code on a Flash sub-agent (code_flash). 主模型规划审查，低级代码交给 Flash 子代理实现。
